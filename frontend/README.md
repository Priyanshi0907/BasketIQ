# BasketIQ — Frontend

Next.js 14 (App Router) + TypeScript + Tailwind frontend for BasketIQ.

See the [project root README](../README.md) for full setup, architecture,
and deployment instructions.

Quick start:

```bash
npm install
cp .env.example .env.local
npm run dev
```
